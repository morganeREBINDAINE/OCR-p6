{% for image in images %}
    <div class="col-sm">
        <div class="image">
            <div class="image-div" style="background-image: url({{ asset('images/tricks/' ~ image.imageName) }})"></div>
            <div class="image-icons">
                <input type="hidden" name="_token" value="{{csrf_token('delete'~ image.id)}}">
                <input type="hidden" name="_image" value="{{image.id}}">
                <button class="trick-image-delete"><i class="fas fa-trash-alt"></i></button>
            </div>
            <div class="mainimg-btn">
                Choisir en image principale
            </div>
        </div>
    </div>
{% endfor %}