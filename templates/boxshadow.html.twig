{% extends 'base.html.twig' %}

{% block body %}
<div class="single-trick position-relative">
    {% set flashes = {'success': app.flashes('success'), 'error': app.flashes('error')} %}

    {% if flashes.success or flashes.error %}
    <div class="modal show" id="flash">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    {% for message in flashes.success %}
                        <div class="alert alert-success">
                            {{ message }}
                        </div>
                    {% endfor %}
                    {% for message in flashes.error %}
                        <div class="alert alert-danger">
                            {{ message }}
                        </div>
                    {% endfor %}
                    <button type="button" class="close" data-dismiss="modal">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            </div>
        </div>
    </div>
    {% endif %}
    <div class="background-img"></div>
    <div class="trick">
        <div class="content">
            <div class="content-container">
                <div class="content-details">
                    {% block children %}{% endblock %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
